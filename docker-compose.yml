services:
  neo4j:
    image: neo4j:5-community
    ports:
      - "7474:7474" # browser UI
      - "7687:7687" # bolt
    environment:
      - NEO4J_AUTH=neo4j/change-me
      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - neo4j-data:/data

volumes:
  neo4j-data:
